[package]
name = "rs2uml"
version = "0.1.0"
edition = "2024"
description = "Generate UML class diagrams to document your rust application"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
